<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <main>

        <nav class="bg-blue-600 p-4 shadow-md">
            <div class="container mx-auto flex justify-between items-center">
                <span class="text-white text-lg font-bold">Sheryians Coding School</span>
                <div>
                    <a href="/user/create" class="px-4 py-2 text-blue-600 bg-white rounded-lg hover:bg-gray-200">Create Post</a>
                    <a href="/user/logout" class="px-4 py-2 text-blue-600 bg-white rounded-lg hover:bg-gray-200">Log Out</a>
                </div>
            </div>
        </nav>

        <div class="flex items-center flex-col gap-10 justify-center mb-28 mt-28">
            <% postdata.forEach(post=>{  %>
                <div class="max-w-sm bg-white rounded-2xl shadow-lg overflow-hidden">
                    <img class="w-full  object-cover" src=<%= post.imageurl %> alt="Posted Image">
                    <div class="p-4">
                        <h3 class="text-lg font-bold text-blue-600">Post Caption</h3>
                        <p class="text-gray-700"><%= post.caption %></p>
                    </div>
                </div>
                <%  }) %>
            </div>
        </main>
    </body>
    </html>


                 
           
                
